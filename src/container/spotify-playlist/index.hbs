{{!
Це код container файлу для index сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
}}
<mein class="mein">
	{{#with data}}
	<div class="mein__content">
		<h1 class="title title--center">{{name}}</h1>

		<a href="/spotify-track-add?playlistId={{playlistId}}" class="add-button">
			<img src="/svg/add_button.svg">
			Додати до плейлистів
		</a>

		<ul class="track__list">
			{{#each tracks}}
			<li class="track">
				<img src="{{image}}" alt="{{name}}">
				<div class="track__content">
					<strong class="track__name">{{name}}</strong>
					<em class="track__author">{{author}}</em>
				</div>
				<a href="/spotify-track-delete?playlistId={{../playlistId}}&trackId={{id}}">
					<img src="/svg/delete_button.svg" class="choose__image">
				</a>
			</li>
			{{/each}}
		</ul>

		<a href="/spotify-library">
			<input type="hidden" name="playlistId" value="{{playlistId}}">
			<input type="hidden" name="trackId" value="{{id}}">
			<button type="submit" class="form__button">
				Моя бібліотека
			</button>
		</a>

		{{!-- <a href="/spotify-library" class="form__button">Моя бібліотека</a> --}}
	</div>
	{{/with}}
</mein>

{{!
Цей код містить лише те, що буде виводитись в

<body>
	Адже наш layout/default.hbs має вже структуру HTML
	}}