{{!
Це код container файлу для index сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
}}
<mein class="mein">
	{{#with data}}
	<div class="mein__content">
		<h1 class="title title--center">Пошук плейлистів</h1>

		<form method="POST" action="/spotify-search" class="search">
			<img src="svg/search_button.svg">
			<input type="search" class="search__input" placeholder="Введіть запит" name="value" value="{{value}}">
			<button class="search__button">Search</button>
		</form>

		<ul class="playlist__list">
			{{#each list}}
			<li class="playlist">
				<img src="{{image}}" alt="{{name}}" height="100px">

				<div class="playlist__content">
					<strong class="playlist__name">{{name}}</strong>
					<em class="playlist__author">Кількість пісень: {{amount}}</em>
				</div>
			</li>
			{{/each}}
		</ul>
	</div>
	{{/with}}
</mein>

{{!
Цей код містить лише те, що буде виводитись в

<body>
	Адже наш layout/default.hbs має вже структуру HTML
	}}